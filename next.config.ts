/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['source.unsplash.com'], // Add any other image domains you'll use
  },
  // Add other config options here
};

module.exports = nextConfig;
